# -----------------------------------
# Node / Bun
# -----------------------------------
/node_modules
/dist
/build
.env
.npm
npm-debug.log
.DS_Store

# -----------------------------------
# Backlog.md specific
# -----------------------------------
# By default, all task files are committed, but if backlog.md 
# creates any temporary files or output reports you want to ignore, 
# you can add them here.
# For example, if you export the board view to a local file:
# backlog-board-export.md 

# -----------------------------------
# Docker
# -----------------------------------
# Ignore any built images/volumes if they were created locally
*.swp
*.bak
*.tmp
# Ignore .env files used by docker-compose for secrets/config
.env
